# Generic deployment package

This package is inert until a separately authorized deployment mission supplies
read-only live preflight evidence. It contains no production connection data.
Rollback restores only the predecessor application/runtime; it never downgrades
the database.
