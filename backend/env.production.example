# Production runtime. The app fails fast when required values below are empty,
# placeholder, wildcard, or development/test-only values.
FLASK_ENV=production
FLASK_DEBUG=False

# Required: set to the real production database URL in the deployment secret store.
DATABASE_URL=

# Required: comma-separated real production origins. Wildcard and localhost are rejected.
CORS_ORIGINS=https://samand.forwarderet.ir
CORS_ALLOW_ALL_ORIGINS=0

# Required: set both to deployment-specific secrets. Do not commit real values.
SECRET_KEY=
JWT_SECRET_KEY=

# Authentication session lifetimes (seconds)
ACCESS_TOKEN_LIFETIME_SECONDS=3600
REFRESH_IDLE_LIFETIME_SECONDS=2592000
SESSION_ABSOLUTE_LIFETIME_SECONDS=7776000
CLOCK_SKEW_SECONDS=60

# Customer password recovery email. Store SMTP credentials in the deployment
# secret store; no real delivery occurs in development, test, or UAT.
FRONTEND_URL=https://samand.forwarderet.ir
CUSTOMER_RECOVERY_EMAIL_ENABLED=1
CUSTOMER_RECOVERY_EMAIL_FROM=
CUSTOMER_RECOVERY_SMTP_HOST=
CUSTOMER_RECOVERY_SMTP_PORT=587
CUSTOMER_RECOVERY_SMTP_USERNAME=
CUSTOMER_RECOVERY_SMTP_PASSWORD=
CUSTOMER_RECOVERY_SMTP_USE_STARTTLS=1
CUSTOMER_RECOVERY_SMTP_USE_SSL=0
CUSTOMER_RECOVERY_SMTP_TIMEOUT_SECONDS=10

# Logging Configuration
LOG_LEVEL=INFO

# SLA Configuration
SLA_HOURS=2

# Monitoring Configuration
ENABLE_MONITORING=True

# Cache Configuration
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300
# Migrations must run as an explicit deployment step before backend startup.
AUTO_MIGRATE_ON_STARTUP=false
DB_CONNECT_TIMEOUT_SECONDS=5
# Required durable private storage outside every release/repository directory.
# Windows example: C:\1-webapp\forwarder-data\case-documents
DOCUMENT_STORAGE_ROOT=/var/lib/forwarder-data/case-documents
