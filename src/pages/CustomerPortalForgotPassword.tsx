import { useState,type FormEvent } from "react"; import { Link } from "react-router";
import CustomerPortalLayout from "@/components/CustomerPortalLayout"; import { Button } from "@/components/ui/button"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { requestCustomerPasswordReset } from "@/lib/customerPortalApi"; import { useI18n } from "@/i18n";
export default function CustomerPortalForgotPassword(){const {t}=useI18n();const [email,setEmail]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);try{await requestCustomerPasswordReset(email);}catch{/* Preserve the same account-existence-safe response. */}finally{setMessage(t("customer.recoveryGeneric"));setBusy(false);}};return <CustomerPortalLayout><Card className="mx-auto max-w-lg"><CardHeader><CardTitle>{t("customer.forgotPassword")}</CardTitle><p className="text-sm text-muted-foreground">{t("customer.recoveryChannelPending")}</p></CardHeader><CardContent><form className="space-y-4" onSubmit={submit}><div><Label htmlFor="recovery-email">{t("common.email")}</Label><Input id="recovery-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div>{message&&<p role="status" className="rounded bg-muted p-3">{message}</p>}<Button className="w-full" disabled={busy}>{t("customer.requestRecovery")}</Button><Button asChild variant="link" className="w-full"><Link to="/customer">{t("customer.backToSignIn")}</Link></Button></form></CardContent></Card></CustomerPortalLayout>}
