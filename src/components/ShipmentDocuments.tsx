import {useCallback,useEffect,useState} from "react";
import {Download,Trash2,Upload} from "lucide-react";
import {Button} from "@/components/ui/button";
import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card";
import {Input} from "@/components/ui/input";
import {deleteShipmentDocument,downloadShipmentDocument,fetchShipmentDocuments,uploadShipmentDocument,type ShipmentDocument} from "@/lib/api";
const stateLabel=(value:string)=>({active:"فعال",superseded:"جایگزین‌شده",deleted:"باطل‌شده"}[value]||value);
export default function ShipmentDocuments({shipmentPublicId}:{shipmentPublicId:string}){const[rows,setRows]=useState<ShipmentDocument[]>([]),[file,setFile]=useState<File|null>(null),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false),[loading,setLoading]=useState(true);const load=useCallback(async()=>{try{setRows((await fetchShipmentDocuments(shipmentPublicId)).data);setError("")}catch(e){setError(e instanceof Error?e.message:"اسناد دریافت نشدند")}finally{setLoading(false)}},[shipmentPublicId]);useEffect(()=>{void load()},[load]);const upload=async()=>{if(!file||!title.trim())return;try{setBusy(true);setError("");const form=new FormData();form.append("file",file);form.append("title",title.trim());if(description.trim())form.append("description",description.trim());await uploadShipmentDocument(shipmentPublicId,form,crypto.randomUUID());setFile(null);setTitle("");setDescription("");await load()}catch(e){setError(e instanceof Error?e.message:"بارگذاری ناموفق بود")}finally{setBusy(false)}};const remove=async(row:ShipmentDocument)=>{const reason=window.prompt("دلیل ابطال سند را وارد کنید:");if(!reason?.trim())return;try{setBusy(true);await deleteShipmentDocument(shipmentPublicId,row.public_id,reason.trim());await load()}catch(e){setError(e instanceof Error?e.message:"ابطال سند ناموفق بود")}finally{setBusy(false)}};return <Card dir="rtl"><CardHeader><CardTitle>اسناد و مدارک حمل</CardTitle><p className="text-sm text-muted-foreground">همه اسناد مجاز محموله—چه متعلق به درخواست و چه خود محموله—در این بخش یکپارچه نمایش داده می‌شوند. اتصال به شماره مرجع اختیاری است.</p></CardHeader><CardContent className="space-y-4">{error&&<p role="alert" className="rounded bg-red-50 p-3 text-red-700">{error}</p>}<div className="grid gap-2 rounded border p-3 sm:grid-cols-2"><Input aria-label="نوع یا دسته تجاری سند" value={title} onChange={e=>setTitle(e.target.value)} placeholder="نوع یا دسته تجاری سند"/><Input aria-label="توضیح سند" value={description} onChange={e=>setDescription(e.target.value)} placeholder="توضیح (اختیاری)"/><Input className="sm:col-span-2" aria-label="انتخاب فایل سند" type="file" accept=".pdf,.jpg,.jpeg,.png,.webp,.docx,.xlsx" onChange={e=>setFile(e.target.files?.[0]||null)}/><Button className="sm:col-span-2" disabled={busy||!file||!title.trim()} onClick={()=>void upload()}><Upload className="ms-2 h-4 w-4"/>{busy?"در حال بارگذاری…":"بارگذاری سند برای محموله"}</Button></div>{loading?<p role="status">در حال دریافت اسناد…</p>:!rows.length?<p className="rounded border border-dashed p-4">هنوز سندی برای این محموله ثبت نشده است.</p>:<div className="space-y-3">{rows.map(row=><article key={row.public_id} className="min-w-0 rounded border p-3"><div className="flex flex-wrap items-start justify-between gap-2"><div className="min-w-0"><strong>{row.business_document_type}</strong><p dir="ltr" className="break-all text-sm">{row.filename}</p></div><span className="rounded bg-slate-100 px-2 py-1 text-xs">{stateLabel(row.lifecycle_state)}</span></div><div className="mt-2 grid gap-1 text-sm sm:grid-cols-2"><p>نسخه {row.version}</p><p>مالک/منشأ: {row.owner==="REQUEST"?"درخواست":"محموله"}</p><p>زمان ثبت: {new Date(row.recorded_at).toLocaleString("fa-IR")}</p><p>ثبت‌کننده: {row.actor||"ثبت نشده"}</p><p>مرجع مرتبط: {row.references.length?row.references.map(x=>`${x.type}: ${x.display_value}`).join("، "):"ندارد"}</p><p>الزام سند مرتبط: {row.requirements.length?row.requirements.map(x=>x.title).join("، "):"ندارد"}</p></div>{row.description&&<p className="mt-2 text-sm text-slate-600">{row.description}</p>}<div className="mt-3 flex flex-wrap gap-2"><Button variant="outline" onClick={()=>void downloadShipmentDocument(shipmentPublicId,row.public_id,row.filename)}><Download className="ms-2 h-4 w-4"/>دریافت</Button>{row.lifecycle_state==="active"&&<Button variant="ghost" disabled={busy} onClick={()=>void remove(row)}><Trash2 className="ms-2 h-4 w-4"/>ابطال</Button>}</div></article>)}</div>}</CardContent></Card>}
