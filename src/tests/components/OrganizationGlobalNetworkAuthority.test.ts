import {readFileSync} from "node:fs";import {join} from "node:path";import {describe,expect,it} from "vitest";
describe("Organization Global Network authority",()=>{const source=readFileSync(join(process.cwd(),"src/components/LogisticsNetworkAdminTab.tsx"),"utf8");it("keeps the organization reference workflow out of the platform-only shell",()=>{expect(source).toContain("!isPlatformAdmin&&<OrganizationGlobalNetworkTab/>");expect(source).toContain("isPlatformAdmin&&<TabsTrigger value=\"types\"");});});
