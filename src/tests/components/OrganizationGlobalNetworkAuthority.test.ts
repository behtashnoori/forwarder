import {readFileSync} from "node:fs";import {join} from "node:path";import {describe,expect,it} from "vitest";
describe("Organization Global Network authority",()=>{const source=readFileSync(join(process.cwd(),"src/pages/AdminPanel.tsx"),"utf8");it("gates adoption navigation and content to Organization Admin",()=>{expect(source).toContain('{isOrganizationAdmin && <TabsTrigger value="organization-global-network"');expect(source).toContain('{isOrganizationAdmin && <TabsContent value="organization-global-network"');expect(source).not.toMatch(/isPlatformAdmin[^\n]+organization-global-network/);});});
