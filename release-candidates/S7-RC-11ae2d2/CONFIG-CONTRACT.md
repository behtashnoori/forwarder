# Configuration contract

No configuration key was added, removed, or renamed by S7-R1. Production requires its existing secure DB, secret, host and explicit CORS-origin contract. The repository supports explicit canonical-origin configuration, but live `samand.forwarderet.ir` CORS/ingress values were not inspected; verify them redacted during separately authorized pre-deployment checks.
