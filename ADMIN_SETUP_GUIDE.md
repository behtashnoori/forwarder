# راهنمای راه‌اندازی کاربر Admin

## ایجاد کاربر Admin

### روش 1: استفاده از اسکریپت Node (توصیه می‌شود، بدون نیاز به Python)

1. در ترمینال، به دایرکتوری پروژه بروید:
```bash
cd c:\1-webapp\1-forwarder
```

2. اسکریپت را اجرا کنید (مستقیماً در دیتابیس ثبت می‌شود):
```bash
node scripts/create-admin-in-db.js
```

این اسکریپت با استفاده از `pg` و `bcryptjs` کاربر admin را در PostgreSQL ایجاد یا به‌روزرسانی می‌کند و خروجی تأیید ثبت در دیتابیس را نشان می‌دهد.

### روش 2: استفاده از اسکریپت Python

```bash
python backend/create_admin.py
```

یا اگر python در دسترس نیست:
```bash
py backend/create_admin.py
```

3. اگر کاربر admin از قبل وجود داشته باشد، از شما می‌پرسد که آیا می‌خواهید رمز عبور را به‌روزرسانی کنید.

### اطلاعات کاربر Admin

- **Username**: `admin`
- **Password**: `Pirooz13@!`
- **Role**: `admin`
- **Full Name**: مدیر سیستم

---

## ورود به سیستم

### ورود به عنوان ادمین

1. از صفحه اصلی روی «ورود کارشناس» کلیک کنید یا به آدرس زیر بروید:
   ```
   http://localhost:5173
   ```

2. با اطلاعات زیر وارد شوید:
   - Username: `admin`
   - Password: `Pirooz13@!`

3. پس از ورود با نقش ادمین، به **پنل ادمین** (`/admin`) هدایت می‌شوید.

### پنل ادمین و مدیریت کاربران

- آدرس پنل ادمین: `http://localhost:5173/admin`
- در پنل ادمین دو بخش وجود دارد: **داشبورد** (آمار کلی) و **مدیریت کاربران** (تعریف، ویرایش، فعال/غیرفعال کردن کاربران با نام، نام خانوادگی، شماره تماس، نام کاربری و رمز عبور).
- آدرس `/user-management` فقط برای کاربران با نقش ادمین قابل دسترسی است و همان پنل ادمین را نمایش می‌دهد.

---

## ایجاد کارشناس جدید

### از طریق API (برای تست)

پس از ورود، یک JWT token دریافت می‌کنید. با استفاده از این token می‌توانید کارشناس ایجاد کنید:

**Endpoint**: `POST /api/user-management/users`

**Headers**:
```
Authorization: Bearer <your_jwt_token>
Content-Type: application/json
```

**Body**:
```json
{
  "username": "expert_new",
  "password": "password123",
  "full_name": "کارشناس جدید",
  "email": "expert@example.com",
  "phone": "09123456789",
  "role": "expert",
  "department": "operations",
  "is_active": true,
  "specializations": []
}
```

**نقش‌های قابل ایجاد توسط Admin**:
- `admin` - مدیر سیستم
- `crm_manager` - مدیر CRM
- `supervisor` - سرپرست
- `business_expert` - کارشناس بازرگانی
- `expert` - کارشناس

### از طریق رابط کاربری

1. وارد پنل مدیریت کاربران شوید (`/user-management`)
2. روی دکمه "کاربر جدید" کلیک کنید
3. فرم را پر کنید:
   - نام کاربری
   - رمز عبور
   - نام کامل
   - ایمیل (اختیاری)
   - شماره تماس (اختیاری)
   - نقش کاربر
   - بخش (اختیاری)
4. روی "ایجاد کاربر" کلیک کنید

---

## تست دسترسی Admin

### بررسی دسترسی‌ها

Admin می‌تواند:
- ✅ ایجاد هر نوع کاربری (admin, crm_manager, supervisor, business_expert, expert)
- ✅ ویرایش تمام کاربران
- ✅ مشاهده تمام کاربران
- ✅ دسترسی به پنل ادمین (`/api/admin/*`)
- ✅ مشاهده گزارش‌ها (`/api/admin/reports/*`)
- ✅ مدیریت قوانین ارجاع
- ✅ مدیریت روش‌های حمل و نقل

### Endpointهای مهم برای Admin

1. **مشاهده کاربران**: `GET /api/user-management/users`
2. **ایجاد کاربر**: `POST /api/user-management/users`
3. **ویرایش کاربر**: `PUT /api/user-management/users/<id>`
4. **مشاهده درخواست‌ها**: `GET /api/admin/shipment-requests`
5. **داشبورد**: `GET /api/admin/dashboard`
6. **گزارش ارجاع**: `GET /api/admin/reports/assignment-summary`

---

## عیب‌یابی

### مشکل: نمی‌توانم وارد شوم

1. بررسی کنید که کاربر admin ایجاد شده باشد:
   ```bash
   python backend/create_admin.py
   ```

2. بررسی کنید که رمز عبور درست باشد: `Pirooz13@!`

3. بررسی کنید که کاربر فعال باشد (`is_active = True`)

### مشکل: نمی‌توانم کارشناس ایجاد کنم

1. بررسی کنید که JWT token معتبر باشد
2. بررسی کنید که نقش کاربر `admin` باشد
3. بررسی کنید که endpoint درست باشد: `POST /api/user-management/users`
4. بررسی کنید که تمام فیلدهای الزامی ارسال شده باشند:
   - username
   - password
   - full_name
   - role

### مشکل: خطای دسترسی

اگر خطای "دسترسی غیرمجاز" دریافت می‌کنید:
1. بررسی کنید که با کاربر admin وارد شده باشید
2. بررسی کنید که JWT token منقضی نشده باشد
3. بررسی کنید که header Authorization درست باشد: `Bearer <token>`

---

## نکات امنیتی

1. **رمز عبور قوی**: رمز عبور `Pirooz13@!` یک رمز عبور قوی است که شامل:
   - حروف بزرگ و کوچک
   - اعداد
   - کاراکترهای خاص
   می‌باشد.

2. **تغییر رمز عبور**: توصیه می‌شود پس از اولین ورود، رمز عبور را تغییر دهید.

3. **مدیریت کاربران**: فقط کاربران با نقش `admin` می‌توانند کاربران دیگر را ایجاد و مدیریت کنند.

4. **لاگ‌ها**: تمام فعالیت‌های admin در لاگ‌های سیستم ثبت می‌شود.

---

## پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های سیستم را بررسی کنید
2. بررسی کنید که دیتابیس درست متصل باشد
3. بررسی کنید که تمام dependencies نصب شده باشند

---

**تاریخ ایجاد**: 2026-02-13
**نسخه**: 1.0
