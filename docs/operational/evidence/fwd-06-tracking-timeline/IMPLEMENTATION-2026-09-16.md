# FWD-06 M1 implementation and qualification record

Candidate: `FWD06-M1-20260916-C1`, existing branch `feature/fwd-06-tracking-timeline`, starting HEAD `0e14d3df7d8d2b704eb7f37138dd09850d9fc9a9`. Result at evidence capture: local product qualification PASS, Git commit/push/fetched-remote verification pending. This is a new local implementation run; earlier BLOCKED and pre-build records remain historical. Proposed M1 bytes were verified before change at SHA256 `6792ADD2CC064A73977386D62263EDA13A0C534CE5A7E118679A1422CD55673A`; the proposal file is unchanged. The Owner's continuation accepted that exact proposal for local FWD-06 only. No human identity was supplied.

## Contract and source ownership

The expert form submits a Gregorian-normalized date plus explicit clock as `time_input_wall`, with exact `time_input_policy=tracking.manual-iran.v1`. Its Persian calendar picker and Gregorian date input update the same normalized date; switching the picker does not convert the chosen clock. The field is blank initially and explicitly labeled «زمان ورود به وقت ایران — تهران». The server resolves `Asia/Tehran`, rejects missing/other policy, invalid date/time, DST gaps and overlaps, and never consults browser or event location timezone for this bounded manual policy. Location still passes the existing Geography resolution, tenant, permission, and snapshot checks.

Accepted literal contract: manual `time_input_source=manual`, `time_input_basis=Asia/Tehran`, `time_input_policy=tracking.manual-iran.v1`; wall format `YYYY-MM-DDTHH:MM[:SS[.ffffff]]` with Gregorian date, no zone suffix, at most 29 characters. Existing offset-bearing API clients submit `occurred_at=YYYY-MM-DDTHH:MM:SS[.ffffff](Z|±HH:MM)`; the server stores `time_input_source=offset`, the exact submitted local wall string, normalized numeric basis (`Z` becomes `+00:00`), and NULL policy. A client `source`, `time_input_source`, `time_input_basis` or `occurred_at_utc` flag is rejected. The offset contract is an existing authenticated expert API compatibility path; it does not grant a new machine actor, manual foreign-zone override, or Agent authority. No machine service principal or Agent endpoint was added.

Tracking owns the event and `OperationalIdempotency` receipt transaction. Each new write has aware canonical `occurred_at_utc`, submitted wall/basis/source/policy (NULL policy only for offset), and derived UTC-naive `occurred_at` mirror. `created_at` remains the system's recording time and is the sole proven-legacy-UTC serialized field. Historical rows retain all five NULL values, raw `occurred_at` and other data without conversion; new Timeline occurrence renders unknown for them. An explicit PostgreSQL constraint checks the three field envelopes, mirror and UTC consistency; a trigger prevents later mutation of a populated time snapshot or its mirror. The service requires complete provenance. No startup migration or backfill was added.

The migration follows sole verified predecessor `20260916_fwd05_quote_response`. Upgrade adds only the five approved nullable event fields and integrity guards. Empty downgrade was executed; once any snapshot exists, downgrade raises before DDL. Application rollback preserves schema, data, receipts and historical reads. An old application cannot be assumed write-compatible with M1 records; stop incompatible writes before application rollback. PostgreSQL test cluster was owned by the qualification launcher, loopback-only, synthetic and stopped after each run. No old DB, customer DB or Production connection was used.

Create and append with `Idempotency-Key` lock the tracking root, reauthorize after the lock, bind the exact payload digest to tenant/operation/resource/key and store result identity/payload in the owner transaction. Same-key/same-payload replay returns the prior response; changed payload conflicts. No-key compatibility remains append-only without replay guarantee. The UI retains its key and input on a failed request. Duplicate `unit_code` returns a field-specific conflict; the original reported first-add failure remains NOT_REPRODUCED_ON_THIS_BASELINE and is not attributed to this UX change.

Internal Timeline reads full authorized subject history while its existing summary remains customer-visible. Public Timeline and `last_recorded_at` use only visible active-unit rows. Public response adds occurrence/recording values but never the five provenance columns, actor, tenant, receipt, internal note or private catalog identity. Sorting remains `(occurred_at,id)` descending with a stable tie break; historical display occurrence remains unknown. A late occurrence does not replace a newer current status. No canonical cutover, correction command, map, GPS, notification policy or product Agent was made.

## Execution evidence and commands

- Native PostgreSQL: `python -B scripts/uat/run_fwd05_disposable_postgres.py backend/tests/test_fwd06_m1_postgresql.py --disable-warnings` — 1 PASS, 29 warnings, no skip/XFAIL. Verified upgrade with an existing historical row and its old column values/relationships unchanged, empty downgrade, re-upgrade, manual and offset round trips, incomplete/inconsistent envelope rejection, mirror immutability, two-way same-key race, revoke-before-replay, populated downgrade refusal, retained row and receipt, and authorized replay after recovery. The disposable cluster was stopped; final diagnostic path: `C:/Users/pc/AppData/Local/Temp/forwarder-fwd05-qualification-b1cf5ce73173445b9f81b01e2bc52fd1`, outside Git.
- Latest focused API/service/location/characterization: `python -B -m pytest -q --tb=short --disable-warnings -p no:cacheprovider backend/tests/test_fwd06_m1_time.py backend/tests/test_multi_unit_tracking_api.py backend/tests/test_multi_unit_tracking_service.py backend/tests/test_public_tracking_timeline.py backend/tests/test_tracking_locations.py backend/tests/test_fwd06_tracking_characterization.py` — 44 PASS, 1 SKIP, 1545 warnings, no XFAIL. The single skip is the opt-in native FWD-05 journey when its cluster variable is absent; its relevant native FWD-06 replacement is run separately above. The independent expected Tehran Instant is `2026-07-15T08:30:00Z` for `2026-07-15T12:00` Tehran. An earlier narrower run had 29 PASS with no skip.
- Browser: `powershell -ExecutionPolicy Bypass -File scripts/uat/run-fwd06-browser-uat.ps1` — 21 PASS on the latest completed run, live backend and Vite with real synthetic login, assignment and status transition, UTC/New York Chromium contexts, create/actionable duplicate/input preservation, Persian day selection and Gregorian round trip, save/reopen, ordinary expert and customer views at desktop/mobile widths. Evidence JSON and screenshots reside outside Git under `C:/Users/pc/AppData/Local/Temp/forwarder-fwd06-browser-f26ed956e01f438d83e42c8e3f92942b`. Earlier failed browser diagnostics exposed missing auth-local state, an asynchronous assertion, and mixed SQLite timezone comparison; those runs are not counted as PASS. The browser fixture uses its owned synthetic SQLite database; PostgreSQL qualification is the independent native run above.
- Architecture governance PASS; current-tree secret scan 0 findings; TypeScript typecheck PASS; Vite build PASS; migration-head tests 19 PASS. The build reports the pre-existing chunk-size and Browserslist freshness warnings.
- Broad FWD-01–05/tracking first run: 174 PASS, 127 SKIP, 2 FAIL from older direct-service naive-time fixtures. Those fixtures were changed to supply explicit UTC snapshots. The fresh run passed: 177 PASS, 127 SKIP, 233427 warnings, 131.41s, no XFAIL. Skips are opt-in native/browser/environment tests, not substituted PASS; independent native PostgreSQL and browser runs above cover the FWD-06 paths.

## Limits retained

Reference28 mapping NOT_PROVEN. Historical replay and Production identity remain blocked/unknown as recorded earlier. Real recipient onboarding, real customer delivery, Production deployment/security and canonical lineage are outside this controlled local qualification. No messages were sent and no product LLM API was called. M1 decision acceptance alone never implied implementation PASS.
