# Preserve exact Windows evidence bytes and their recorded SHA-256 hashes.
** -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
# Raw tool output is retained verbatim, including generated whitespace.
*.log -diff
*.xml -diff
