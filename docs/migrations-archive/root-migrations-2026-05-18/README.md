# Archived root migrations

Date archived: 2026-05-18

These files were moved out of the executable root `migrations/` path during Phase 3 migration cleanup. The canonical Alembic migration path is `backend/migrations`.

This archive is retained for historical/DBA review only and must not be used as an Alembic script location. Do not add new migration files here.

Important notes:

- The first three version files are exact duplicates of canonical files under `backend/migrations/versions`.
- Three later shared-name files are older divergent copies superseded by canonical files under `backend/migrations/versions`.
- `54ea21ea0d9f_add_transport_method_columns.py` is a root-only autogenerated migration with broad destructive operations; it is not part of the canonical chain and was archived rather than deleted so history is preserved for manual review if a production database ever reports that revision.
