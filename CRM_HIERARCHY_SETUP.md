# راهنمای راه‌اندازی سیستم مدیریت سلسله مراتبی CRM

## 📋 خلاصه تغییرات

سیستم مدیریت سلسله مراتبی CRM با ویژگی‌های زیر پیاده‌سازی شده است:

### 🏗️ ساختار سلسله مراتبی
```
مدیر سیستم (admin)
├── مدیر CRM (crm_manager)
│   ├── کارشناس بازرگانی 1 (business_expert)
│   ├── کارشناس بازرگانی 2 (business_expert)
│   └── کارشناس بازرگانی 3 (business_expert)
└── سرپرست (supervisor)
    └── کارشناس (expert)
```

### 🚛 روش‌های حمل و نقل
- حمل و نقل جاده‌ای
- حمل و نقل ریلی
- حمل و نقل هوایی
- حمل و نقل دریایی
- پیک سریع

### ⚙️ منطق ارجاع خودکار
- ارجاع بر اساس تخصص کارشناسان
- توزیع بار کاری
- اولویت‌بندی درخواست‌ها
- قوانین قابل تنظیم

## 🚀 راه‌اندازی سیستم

### مرحله 1: اجرای Migration ها

```bash
cd backend
python run_migrations.py
```

### مرحله 2: راه‌اندازی کامل (توصیه شده)

```bash
cd backend
python setup_crm_hierarchy.py
```

این script به صورت خودکار:
- Migration ها را اجرا می‌کند
- داده‌های اولیه را ایجاد می‌کند
- سیستم را بررسی می‌کند

## 👥 کاربران پیش‌فرض ایجاد شده

### مدیر CRM
- **نام کاربری**: `crm_manager`
- **رمز عبور**: `crm123`
- **دسترسی**: مدیریت کارشناسان بازرگانی

### کارشناسان بازرگانی
- **نام کاربری**: `business_expert1`, `business_expert2`, `business_expert3`
- **رمز عبور**: `business123`
- **تخصص‌ها**: 
  - Expert 1: حمل جاده‌ای، پیک سریع
  - Expert 2: حمل هوایی، حمل دریایی
  - Expert 3: حمل ریلی، حمل جاده‌ای

## 🌐 دسترسی به سیستم

### صفحات اصلی
- **مدیریت کاربران**: `http://localhost:5173/user-management`
- **داشبورد CRM**: `http://localhost:5173/crm`
- **کنسول کارشناس**: `http://localhost:5173/expert`

### API Endpoints جدید
- `GET /api/user-management/users` - لیست کاربران
- `POST /api/user-management/users` - ایجاد کاربر جدید
- `GET /api/user-management/transport-methods` - روش‌های حمل
- `GET /api/user-management/assignment-rules` - قوانین ارجاع
- `GET /api/user-management/assignment-statistics` - آمار ارجاع

## 🔧 ویژگی‌های کلیدی

### 1. مدیریت سلسله مراتبی
- مدیر CRM می‌تواند کارشناس بازرگانی ایجاد کند
- هر کارشناس زیر نظر مدیر خود قرار دارد
- کنترل دسترسی بر اساس نقش

### 2. تخصص‌های کارشناسان
- هر کارشناس می‌تواند در چندین روش حمل تخصص داشته باشد
- یک تخصص اصلی برای هر کارشناس
- سطح مهارت: مبتدی، متوسط، پیشرفته، متخصص

### 3. ارجاع خودکار
- هنگام ایجاد درخواست جدید، سیستم بهترین کارشناس را انتخاب می‌کند
- بر اساس تخصص، بار کاری، و قوانین تعریف شده
- امکان ارجاع دستی توسط مدیران

### 4. قوانین ارجاع قابل تنظیم
- ارجاع بر اساس روش حمل
- توزیع بار کاری
- اولویت‌بندی درخواست‌ها
- قوانین سفارشی

## 📊 آمار و گزارش‌گیری

### داشبورد مدیریت
- تعداد کل ارجاعات
- ارجاعات خودکار vs دستی
- بار کاری کارشناسان
- عملکرد سیستم

### لاگ ارجاع‌ها
- ثبت تمام ارجاع‌ها
- دلیل ارجاع
- کارشناس مسئول
- زمان ارجاع

## 🔍 عیب‌یابی

### مشکلات رایج

#### 1. خطای Migration
```bash
# بررسی وضعیت migration
cd backend
flask db current
flask db history
```

#### 2. خطای Import
```bash
# بررسی imports
python -c "from models import ExpertUser, TransportMethod"
```

#### 3. خطای دسترسی
- بررسی token در localStorage
- بررسی نقش کاربر
- بررسی دسترسی‌های API

### لاگ‌ها
- لاگ‌های سیستم در `instance/logs/`
- لاگ‌های ارجاع در جدول `assignment_log`
- لاگ‌های کاربری در جدول `expert_console_log`

## 🎯 استفاده عملی

### برای مدیر CRM:
1. ورود با `crm_manager` / `crm123`
2. رفتن به `/user-management`
3. ایجاد کارشناس بازرگانی جدید
4. تعریف تخصص‌های کارشناس
5. تنظیم قوانین ارجاع

### برای کارشناس بازرگانی:
1. ورود با `business_expert1` / `business123`
2. مشاهده درخواست‌های ارجاع شده
3. مدیریت درخواست‌های مربوط به تخصص‌های خود

### ارجاع خودکار:
- درخواست‌های جدید به صورت خودکار ارجاع می‌شوند
- بر اساس تخصص و بار کاری کارشناسان
- امکان ارجاع دستی در صورت نیاز

## 📞 پشتیبانی

در صورت بروز مشکل:
1. بررسی لاگ‌های سیستم
2. بررسی وضعیت دیتابیس
3. اجرای مجدد setup script
4. بررسی دسترسی‌های کاربر

---

**نکته**: این سیستم بر اساس استانداردهای CRM و معماری بهینه طراحی شده است و قابلیت توسعه و سفارشی‌سازی دارد.
