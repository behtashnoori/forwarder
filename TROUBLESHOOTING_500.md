# رفع خطای 500 روی health، transport-methods و provinces

## چه مشکلی بود؟

پس از ریستارت یا با دیتابیس خالی، این اندپوینت‌ها گاهی **500 (Internal Server Error)** برمی‌گرداندند:

- `GET /api/health`
- `GET /api/transport-methods`
- `GET /api/provinces`

در فرانت هم پیام کلی نمایش داده می‌شد:  
«Backend health check failed (is backend running on PORT from .env?)» بدون جزئیات واقعی خطا.

**دلایل اصلی:**

1. **Health فقط به اتصال DB نگاه می‌کرد** و وجود جدول‌های ضروری (`province`, `transport_method`) را چک نمی‌کرد.
2. **جدول‌ها خالی یا ناموجود بودند** و درخواست اول به `/api/transport-methods` یا `/api/provinces` منجر به خطای داخلی می‌شد.
3. **استارت بک‌اند بدون اعتبارسنجی کامل** انجام می‌شد؛ سرور بالا می‌آمد ولی اولین درخواست‌ها 500 می‌دادند.
4. **خطاها لاگ نمی‌شدند** و پاسخ 500 بدون جزئیات (مثلاً در حالت dev) برمی‌گشت.

---

## چطور دوباره تولید کنیم؟

1. بک‌اند را متوقف کنید.
2. دیتابیس را خالی کنید یا جداول `province` و `transport_method` را حذف کنید (یا یک دیتابیس تازه بسازید).
3. بک‌اند را با `python -m backend.run` بالا بیاورید.

**قبل از اصلاحات:** سرور بالا می‌آمد؛ با `curl http://localhost:<PORT>/api/health` یا `/api/provinces` یا `/api/transport-methods` ممکن بود 500 بگیرید.

**بعد از اصلاحات:** اگر جداول یا مایگریشن‌ها درست نباشند، بک‌اند در **استارت** با پیام واضح خارج می‌شود (fail-fast) و دیگر روی اولین درخواست 500 نمی‌گیرید.

---

## چه تغییراتی انجام شد؟

### 1) لاگ و پاسخ خطا (بدون 500 سایلنت)

- هندلر سراسری 500 در Flask:
  - همیشه **traceback** کامل در لاگ چاپ می‌شود.
  - در **حالت توسعه (dev)** پاسخ JSON شامل `error`, `details` (شامل traceback), و `path` برمی‌گردد تا ریشه خطا پیدا شود.

### 2) اندپوینت `/api/health`

- فقط **اتصال دیتابیس** با `SELECT 1` چک می‌شود تا با بالا آمدن سایت خطای 500 نمایش داده نشود.
- **اگر DB قطع باشد:** وضعیت **500** با `{ "status": "error", "database": "not_ready", "message": "..." }`.
- **اگر DB متصل باشد:** همیشه **200** با `{ "status": "ok", "database": "connected", "port": <PORT> }`.
- برای چک کامل (وجود جداول): `GET /api/health?readiness=1` — در پاسخ فیلد `tables_ready` اضافه می‌شود.

### 3) `/api/transport-methods` و `/api/provinces`

- اگر جدول خالی باشد: پاسخ **200** با آرایه خالی (`[]` یا لیست‌های خالی در ساختار transport-methods). دیگر به خاطر «خالی بودن» 500 برنمی‌گردد.
- فقط در صورت **خطای واقعی دیتابیس/سرور** 500 برمی‌گردد؛ در آن صورت traceback در لاگ چاپ می‌شود و پاسخ JSON شامل `error` و `message` است.

### 4) استارت fail-fast

- در `backend.run` قبل از بالا آمدن سرور:
  - اتصال DB چک می‌شود.
  - مایگریشن‌ها اجرا می‌شوند.
  - وجود جداول حیاتی (`province`, `transport_method`) بررسی می‌شود.
  - در صورت خالی بودن این جداول، **seed اولیه** (استان‌ها و روش‌های حمل) اجرا می‌شود.

اگر هر کدام از این مراحل شکست بخورد، با **exit(1)** و پیام واضح خارج می‌شود؛ دیگر «سرور بالا بیاید و اولین درخواست 500 بدهد» پیش نمی‌آید.

### 5) فرانت

- **Health check (مثلاً در `App.tsx`):**
  - اگر پاسخ 500 با بدنه JSON باشد، پیام واقعی از `message` یا `error` یا `database === "not_ready"` در کنسول نمایش داده می‌شود.
  - آدرس دقیق health در حالت dev لاگ می‌شود: `Health check URL: ...`
- **LocationForm (استان‌ها / روش‌های حمل):**
  - در صورت خطا در دریافت transport methods یا provinces، یک پیام یکسان و کاربرپسند نشان داده می‌شود:  
    «خطا در دریافت اطلاعات پایه (استان‌ها/روش‌های حمل). لطفاً بک‌اند را بررسی کنید.»  
  - traceback/خطای کامل همچنان در کنسول باقی می‌ماند.

---

## چطور بررسی کنیم؟

پورت را از `.env` یا مقدار پیش‌فرض (مثلاً 8000) بگیرید و در همین دستورات جایگزین کنید.

```bash
# سلامت و آمادگی بک‌اند (باید 200 و database: connected باشد)
curl http://localhost:<PORT>/api/health

# لیست استان‌ها (باید 200 و آرایه، حتی خالی)
curl http://localhost:<PORT>/api/provinces

# روش‌های حمل (باید 200 و ساختار با international_methods و domestic_methods)
curl http://localhost:<PORT>/api/transport-methods
```

مثال خروجی سالم برای health:

```json
{"status":"ok","database":"connected","port":8081}
```

اگر دیتابیس یا جدول آماده نباشد، health با 500 و مثلاً چنین چیزی برمی‌گردد:

```json
{"status":"error","database":"not_ready","message":"..."}
```

با این تغییرات، خطاهای 500 روی این سه اندپوینت باید قابل ردیابی و با استارت fail-fast و seed، در شرایط عادی دیگر رخ ندهند.
